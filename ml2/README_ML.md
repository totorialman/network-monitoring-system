# Мультисистемное приложение для сбора и предварительного анализа сетевого трафика

Программа для обучения модели без учителя на сетевом трафике и детекции аномалий в реальном времени. При обнаружении аномалии формируется архив с логом трафика за последние N минут и отправляется на веб-сервер.

**Поддержка больших pcap файлов:** Обучение возможно из pcap/pcapng файлов любого размера (включая 100+ ГБ) благодаря потоковому чтению — пакеты обрабатываются по одному без загрузки всего файла в память.

---

## Содержание

- [Описание](#описание)
- [Как это работает](#как-это-работает)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Запуск](#запуск)
- [Обучение из pcap файла](#обучение-из-pcap-файла)
- [Параметры командной строки](#параметры-командной-строки)
- [Структура проекта](#структура-проекта)
- [Признаки и агрегация](#признаки-и-агрегация)
- [Настройка чувствительности](#настройка-чувствительности)
- [Устранение неполадок](#устранение-неполадок)

---

## Описание

Программа работает в **двух режимах**:

1. **Обучение** — захват трафика в течение заданного времени (или чтение из pcap файла), агрегация по временным окнам, обучение модели Isolation Forest и сохранение модели в файл.
2. **Детекция** — непрерывный захват трафика в реальном времени, агрегация по тем же временным окнам, предсказание аномалий; при срабатывании выводится информация в консоль и (опционально) на веб-сервер отправляется ZIP-архив с логом трафика за последние N минут.

Данные агрегируются **только по временному окну** фиксированной длины (в секундах). Размер окна и чувствительность детектора задаются в конфигурационном файле или через аргументы командной строки.

---

## Как это работает

### Общая схема

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         РЕЖИМ ОБУЧЕНИЯ (train)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  Источник данных (интерфейс ИЛИ pcap файл):                              │
│                                                                         │
│  Вариант A: Сетевой интерфейс → Захват пакетов →                        │
│  Вариант B: Pcap файл → Потоковое чтение →                              │
│                                                                         │
│  → Извлечение признаков → Агрегация по окнам →                          │
│  → Обучение Isolation Forest → Сохранение модели                        │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        РЕЖИМ ДЕТЕКЦИИ (detect)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  Сетевой интерфейс → Захват пакетов → Извлечение признаков              │
│       ↓                      ↓                                            │
│  TrafficLogger     TimeWindowAggregator → Модель → Аномалия?             │
│  (буфер N минут)         (окна)              ↓                           │
│       ↓                    ↓              Да → Вывод в консоль +         │
│       └────────────────────┴──────────────→ ZIP(трафик за N мин) →      │
│                                    POST на веб-сервер (если включено)    │
└─────────────────────────────────────────────────────────────────────────┘
```

### Режим обучения (train)

#### Вариант A: Обучение с сетевого интерфейса

1. Запускается захват пакетов с указанного интерфейса на заданное время (минуты).
2. Из каждого пакета извлекаются признаки (IP, порты, протокол, длина, TTL и т.д.).
3. Пакеты группируются в **временные окна** фиксированной длины (например, 5 секунд). Для каждого окна считаются агрегированные метрики: число пакетов, пакетов/сек, уникальные IP/порты, статистики по протоколам и длине пакетов.
4. По этим метрикам обучается **Isolation Forest** (без учителя). Модель и нормализатор (StandardScaler) сохраняются в файл (например, `anomaly_model.pkl`).

#### Вариант B: Обучение из pcap файла (для больших дампов)

1. Pcap файл открывается через `PcapReader` (scapy) — **потоковое чтение**, пакет за пакетом.
2. **Весь файл НЕ загружается в память** — каждый пакет обрабатывается и сразу передаётся в агрегатор.
3. Из каждого пакета извлекаются признаки (аналогично захвату с интерфейса).
4. Пакеты группируются в **временные окна** и агрегируются.
5. Агрегированные данные (окна) накапливаются в памяти — их объём мал (одно окно на каждые N секунд трафика).
6. По завершении чтения обучается **Isolation Forest** и модель сохраняется.

> **Пример:** Для 140 ГБ pcap файла с 5-секундными окнами в памяти хранятся только агрегированные окна (несколько тысяч записей), а не миллионы пакетов.

### Режим детекции (detect)

1. Загружается сохранённая модель и (опционально) порог по `anomaly_score`.
2. Запускается непрерывный захват пакетов. Каждый пакет:
   - добавляется в **TrafficLogger** — кольцевой буфер, хранящий трафик за последние N минут (для архива при аномалии);
   - передаётся в **TimeWindowAggregator** — по мере заполнения окон получаются агрегированные векторы.
3. Для каждого завершённого окна модель выдаёт предсказание (норма/аномалия) и значение `anomaly_score`.
4. Если зафиксирована аномалия:
   - в консоль выводится подробная информация по окну;
   - при включённой настройке формируется ZIP-архив с JSONL-логом трафика за последние N минут и отправляется POST-запросом на веб-сервер (URL задаётся в конфиге).

### Демонстрационный веб-сервер (alert_server.py)

Простой Flask-сервер принимает POST-запросы на `/anomaly` с полями:

- `message` — текстовое описание аномалии;
- `file` — ZIP-архив с логом трафика.

Сервер сохраняет архив в каталог `uploads/` и выводит в консоль сообщение о полученной аномалии и путь к файлу. Этого достаточно для демонстрации приёма алертов.

---

## Установка

### Требования

- Python 3.7+
- Права root (sudo) для захвата сетевых пакетов (не требуется для чтения pcap файлов)

### Установка зависимостей

Из каталога с исходным кодом:

```bash
pip install -r requirements.txt
```

В `requirements.txt` указаны: `scapy`, `scikit-learn`, `numpy`, `requests`, `flask`.

---

## Конфигурация

Параметры вынесены в файл **`config.py`**. Их можно менять прямо в файле; значения из конфига используются как умолчания для CLI.

### Блок `training` (режим обучения)

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `interface` | Сетевой интерфейс | `"eth0"` |
| `duration_minutes` | Длительность захвата в минутах | `5` |
| `window_size_seconds` | Размер временного окна (сек) | `5.0` |
| `model_path` | Путь к файлу модели | `"anomaly_model.pkl"` |
| `pcap_progress_interval` | Интервал вывода прогресса при чтении pcap (пакетов) | `100000` |

### Блок `detection` (режим детекции)

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `interface` | Сетевой интерфейс | `"eth0"` |
| `window_size_seconds` | Размер временного окна (сек) | `5.0` |
| `model_path` | Путь к файлу модели | `"anomaly_model.pkl"` |
| `score_threshold` | Порог по anomaly_score (`None` — бинарное предсказание модели). Чем ниже — тем выше чувствительность | `None` |
| `traffic_log_minutes` | Сколько минут трафика хранить и упаковывать в ZIP при аномалии | `5` |
| `alert_server_url` | URL для отправки архива (например, `http://127.0.0.1:5000/anomaly`) | `"http://127.0.0.1:5000/anomaly"` |
| `send_zip_on_anomaly` | Отправлять ли ZIP на веб-сервер при аномалии | `True` |

### Блок `model`

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `contamination` | Ожидаемая доля аномалий в обучающей выборке (Isolation Forest) | `0.01` |

Аргументы командной строки переопределяют соответствующие значения из конфига для текущего запуска.

---

## Запуск

Все команды ниже предполагают, что вы находитесь в каталоге с `main.py`, `config.py` и остальными модулями.

### 1. Режим обучения с сетевого интерфейса

Обучить модель на трафике с интерфейса по умолчанию и параметрами из `config.py`:

```bash
sudo python main.py --mode train
```

С указанием интерфейса, длительности и размера окна:

```bash
sudo python main.py --mode train --interface eth0 --duration 10 --window-size 5
```

Сохранение модели в отдельный файл:

```bash
sudo python main.py --mode train --model my_model.pkl --duration 5
```

Обучение должно проходить в период «нормального» трафика, без явных аномалий.

### 2. Режим детекции

Запуск детекции с моделью и параметрами по умолчанию (из `config.py`):

```bash
sudo python main.py --mode detect
```

С указанием модели, интерфейса и размера окна (размер окна должен совпадать с тем, на котором обучали):

```bash
sudo python main.py --mode detect --model my_model.pkl --interface eth0 --window-size 5
```

С заданием порога чувствительности:

```bash
sudo python main.py --mode detect --score-threshold -0.4
```

Остановка: **Ctrl+C**. В конце выведется количество обработанных окон и обнаруженных аномалий.

### 3. Демонстрация отправки архива на веб-сервер

1. В одном терминале запустите демонстрационный сервер приёма архивов:

```bash
python alert_server.py
```

Сервер слушает порт 5000 (по умолчанию `http://0.0.0.0:5000`). Убедитесь, что в `config.py` указано:
- `detection.alert_server_url = "http://127.0.0.1:5000/anomaly"`
- `detection.send_zip_on_anomaly = True` (при необходимости).

2. В другом терминале запустите детектор:

```bash
sudo python main.py --mode detect
```

При срабатывании аномалии в консоли детектора появится сообщение об отправке архива; в консоли `alert_server.py` — сообщение о полученной аномалии и путь к сохранённому ZIP в каталоге `uploads/`.

---

## Обучение из pcap файла

### Поддерживаемые форматы

- `.pcap` — стандартный формат libpcap
- `.pcapng` — расширенный формат pcapng
- `.cap` — формат capture

### Базовое использование

Обучение модели из pcap файла (потоковое чтение, подходит для файлов любого размера):

```bash
python main.py --mode train --pcap /path/to/dump.pcap
```

> **Важно:** Для чтения pcap файлов **не требуются права root** (sudo).

### Обучение из нескольких pcap файлов (для больших дампов)

Если дамп с коммутатора разбит на несколько файлов (например, `dump_001.pcap`, `dump_002.pcap`), их можно обработать за один запуск. Параметр `--pcap` можно указывать **несколько раз**:

```bash
python main.py --mode train \
  --pcap /data/switch/dump_001.pcap \
  --pcap /data/switch/dump_002.pcap \
  --pcap /data/switch/dump_003.pcap \
  --model switch_model.pkl \
  --window-size 5
```

**Как это работает:**
1. Каждый файл читается последовательно (потоково, без загрузки в память)
2. Агрегированные окна из всех файлов накапливаются в общий список
3. По завершении всех файлов модель обучается **один раз** на всех собранных данных

> **Память:** В ОЗУ хранятся только агрегированные окна (каждое — ~20 чисел). Для 140 ГБ трафика и окна 5 секунд — ~500–700 тыс. окон (~100–200 МБ). Файлы 100+ ГБ не загружаются в память целиком.

### С указанием размера окна и пути к модели

```bash
python main.py --mode train --pcap /path/to/dump.pcap --window-size 10 --model switch_model.pkl
```

### С лимитом пакетов (для тестирования)

Обработать только первые N пакетов из одного или каждого из файлов:

```bash
python main.py --mode train --pcap /path/to/dump.pcap --max-packets 1000000
```

### Пример вывода при чтении одного pcap

```
============================================================
Обучение модели детекции аномалий (из pcap файла)
Файл: /data/switch_dump.pcap
Размер окна: 5.0 секунд
============================================================

Открытие pcap файла: /data/switch_dump.pcap
Размер файла: 140.25 ГБ
Начало потокового чтения...

Прогресс: 100,000 пакетов прочитано, 98,542 обработано, 1,458 пропущено | Скорость: 45,230 pkt/s | Время: 2s
Прогресс: 200,000 пакетов прочитано, 197,103 обработано, 2,897 пропущено | Скорость: 46,100 pkt/s | Время: 4s
...

============================================================
Чтение pcap файла завершено
Всего пакетов в файле: 150,234,567
Обработано пакетов: 148,901,234
Пропущено пакетов: 1,333,333
Время обработки: 3245.67 секунд
Средняя скорость: 46,287 пакетов/сек
============================================================

Собрано 648,901 агрегированных образцов из 148,901,234 пакетов
Обучение модели на 648,901 образцах...
Модель обучена. Использовано 18 признаков.

Обучение завершено успешно!
Модель сохранена в: anomaly_model.pkl
```

### Пример вывода при чтении нескольких pcap файлов

```
========================================================================
Обучение модели детекции аномалий (из pcap файлов)
Всего файлов: 3
  - /data/switch/dump_001.pcap
  - /data/switch/dump_002.pcap
  - /data/switch/dump_003.pcap
Размер окна: 5.0 секунд
========================================================================

====================================================================
[1/3] Обработка файла: /data/switch/dump_001.pcap
====================================================================
Открытие pcap файла: /data/switch/dump_001.pcap
...
  → Накоплено агрегированных окон: 210,445

====================================================================
[2/3] Обработка файла: /data/switch/dump_002.pcap
====================================================================
...
  → Накоплено агрегированных окон: 430,102

====================================================================
[3/3] Обработка файла: /data/switch/dump_003.pcap
====================================================================
...
  → Накоплено агрегированных окон: 648,901

Собрано 648,901 агрегированных образцов из 3 pcap-файла(ов)
Обучение модели на 648,901 образцах...
Модель обучена. Использовано 18 признаков.

Обучение завершено успешно!
Модель сохранена в: switch_model.pkl
```

### Как работает потоковое чтение

```
┌─────────────────────────────────────────────────────────────┐
│                    Потоковое чтение pcap                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Pcap файл (140 ГБ на диске)                                │
│  │                                                           │
│  ├─ Пакет 1 → extract_features() → aggregator → окно 1      │
│  ├─ Пакет 2 → extract_features() → aggregator → окно 1      │
│  ├─ Пакет 3 → extract_features() → aggregator → окно 1      │
│  ├─ ...                                                      │
│  ├─ Пакет N → extract_features() → aggregator → окно 2 ✓    │
│  │                        (окно 1 завершено, в памяти)       │
│  ├─ Пакет N+1 → ... → окно 2                                │
│  └─ ...                                                      │
│                                                             │
│  В памяти хранятся ТОЛЬКО:                                  │
│  - Текущее окно (пакеты в пределах window_size)             │
│  - Завершённые агрегированные окна (маленькие векторы)      │
│                                                             │
│  Потребление памяти: ~несколько МБ независимо от            │
│  размера pcap файла!                                        │
└─────────────────────────────────────────────────────────────┘
```

### Рекомендации для больших файлов

1. **Размер окна:** Для длительных записей (часы/дни) увеличьте `--window-size` (например, 30-60 секунд) — это уменьшит количество агрегированных образцов и ускорит обучение.

2. **Лимит пакетов:** Для предварительного тестирования используйте `--max-packets` чтобы обработать только часть файла.

3. **Прогресс:** Интервал вывода прогресса настраивается в `config.py` через `training.pcap_progress_interval`.

4. **Время обработки:** Скорость чтения зависит от скорости диска и CPU. Для 140 ГБ файла ожидайте несколько часов обработки.

5. **Диск:** Убедитесь, что на диске достаточно места для сохранения модели (обычно несколько МБ).

---

## Параметры командной строки

| Параметр | Короткая форма | Описание | По умолчанию |
|----------|----------------|----------|--------------|
| `--mode` | `-m` | Режим: `train` или `detect` | **обязательный** |
| `--interface` | `-i` | Сетевой интерфейс | из `config.py` |
| `--model` | `-M` | Путь к файлу модели | из `config.py` |
| `--duration` | `-d` | Длительность обучения (минуты), только для `train` с интерфейсом | из `config.py` |
| `--window-size` | `-w` | Размер временного окна (секунды) | из `config.py` |
| `--score-threshold` | `-t` | Порог по anomaly_score для `detect` (ниже — выше чувствительность) | из `config.py` |
| `--pcap` | `-p` | Путь к pcap файлу для обучения (можно указывать **несколько раз**) | `None` |
| `--max-packets` | — | Лимит пакетов на файл при чтении из pcap | `None` (все) |

Пример полного запуска детекции:

```bash
sudo python main.py --mode detect --interface eth0 --model anomaly_model.pkl --window-size 5 --score-threshold -0.4
```

Пример обучения из нескольких pcap файлов (например, дамп с коммутатора):

```bash
python main.py --mode train --pcap dump_001.pcap --pcap dump_002.pcap --model switch_model.pkl --window-size 10
```

---

## Структура проекта

```
src/
├── main.py             # Точка входа, CLI, вызов train/detect
├── config.py           # Конфигурация (training, detection, model)
├── packet_capture.py   # Захват пакетов (scapy), извлечение признаков
├── pcap_reader.py      # Потоковое чтение pcap файлов (для больших дампов)
├── aggregation.py      # TimeWindowAggregator — агрегация по временным окнам
├── anomaly_detector.py # Обучение и предсказание (Isolation Forest, StandardScaler)
├── alerting.py         # TrafficLogger, сбор ZIP, отправка на веб-сервер
├── alert_server.py     # Демо веб-сервер для приёма архивов (Flask)
├── requirements.txt    # Зависимости Python
└── README.md           # Документация
```

---

## Признаки и агрегация

### Признаки из каждого пакета

| Признак | Описание |
|---------|----------|
| `timestamp` | Время захвата (из pcap или системное) |
| `src_mac`, `dst_mac` | MAC-адреса |
| `vlan`, `eth_type` | VLAN, тип Ethernet |
| `src_ip`, `dst_ip` | IP-адреса (IPv4/IPv6/ARP) |
| `proto`, `ttl` | Протокол, TTL |
| `src_port`, `dst_port` | Порты (TCP/UDP) |
| `tcp_flags` | Флаги TCP |
| `icmp_type`, `icmp_code` | ICMP |
| `length` | Длина пакета |

### Агрегация по временному окну

Пакеты группируются в окна фиксированной длины (например, 5 секунд). Для каждого окна считаются:

- число пакетов, длительность, пакетов/сек;
- уникальные src/dst IP, уникальные src/dst порты;
- счётчики по протоколам (TCP, UDP, ICMP);
- средняя/мин/макс длина пакета, средний TTL;
- количество уникальных TCP-флагов, число ICMP-пакетов.

Эти метрики образуют вектор признаков для обучения и предсказания. Размер окна задаётся в `config.py` или через `--window-size` и должен быть одинаковым при обучении и детекции.

---

## Настройка чувствительности

- **Через CLI:** `--score-threshold <значение>` (например, `-0.4`).
- **Через config.py:** `detection.score_threshold = -0.4` (или `None` для бинарного предсказания модели).

Для Isolation Forest:
- типичные «нормальные» score около -0.2…0.0;
- чем ниже score, тем более «аномально» образец;
- чем ниже заданный порог, тем больше срабатываний (выше чувствительность).

Рекомендуется сначала посмотреть значения score на своём трафике и затем задать порог по необходимости.

---

## Пример вывода при аномалии

```
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
ОБНАРУЖЕНА АНОМАЛИЯ
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
Временное окно: 2025-12-10 18:18:10.000 - 2025-12-10 18:18:15.000
Длительность окна: 5.000 секунд
Количество пакетов: 1500
Пакетов в секунду: 300.00
Уникальных IP источников: 50
...
Оценка аномальности: -0.5742
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

Если включена отправка на веб-сервер, дополнительно выводится сообщение об отправке ZIP или об ошибке (с путём к локальному архиву).

---

## Устранение неполадок

| Проблема | Решение |
|----------|---------|
| Интерфейс недоступен | Проверить список интерфейсов: `ip link show` или `ifconfig`, указать правильный `--interface` или в `config.py`. |
| Permission denied при захвате | Запускать с `sudo`. Для чтения pcap файлов sudo не требуется. |
| Файл модели не найден | Сначала выполнить обучение: `python main.py --mode train --pcap dump.pcap` или `sudo python main.py --mode train`. |
| Мало данных для обучения | Увеличить `--duration` или проверить наличие трафика на интерфейсе. Для pcap — проверить, что файл содержит пакеты. |
| Архив не отправляется на сервер | Проверить, что запущен `alert_server.py`, в конфиге верный `alert_server_url` и `send_zip_on_anomaly = True`. При ошибке сети архив остаётся локально — путь выводится в консоль. |
| Pcap файл не найден | Проверить путь к файлу. Поддерживаются форматы `.pcap`, `.pcapng`, `.cap`. |
| Медленное чтение pcap | Скорость зависит от диска и CPU. Для SSD ожидайте ~40-60K пакетов/сек. Для ускорения можно увеличить `--window-size`. |

---